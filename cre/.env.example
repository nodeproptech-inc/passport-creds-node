# =============================================================
# PassportKit Node — Chainlink CRE service
# Kept in scope. Exact role in the new PassportKit design is TBD
# (e.g. CRE triggers claim issuance after World proof / acts as keeper).
# Wire the contract targets once the CRE↔contract flow is decided.
# =============================================================

BACKEND_BASE_URL=http://localhost:3001
CRE_WORKFLOW_CALLBACK_URL=http://localhost:3001/cre/workflow-result

# --- Network (Ethereum Sepolia) ---
RPC_URL=https://sepolia.infura.io/v3/<your-key>
CHAIN_ID=11155111

# CRE node operator key (testnet). NEVER commit the real value.
PRIVATE_KEY=0x

# --- Contracts the CRE interacts with (fill once the flow is decided) ---
ELIGIBILITY_GATE_ADDRESS=0x
CLAIM_ISSUER_ADDRESS=0x
COMPLIANCE_PASSPORT_ADDRESS=0x

# true = mock contract adapter (simulated tx hashes)
DEMO_MODE=true
